@model IEnumerable<DesapegAutoWeb.Models.MarcaViewModel>
@{
    ViewData["Title"] = "Administracao de Marcas";
    var marcas = Model?.ToList() ?? new List<DesapegAutoWeb.Models.MarcaViewModel>();
}

<div class="da-page">
    <h1 class="da-title">Administracao</h1>

    @if (TempData["SuccessMessage"] is string successMessage)
    {
        <div class="alert alert-success" role="alert">@successMessage</div>
    }

    @if (TempData["ErrorMessage"] is string errorMessage)
    {
        <div class="alert alert-danger" role="alert">@errorMessage</div>
    }

    <div class="da-split-layout">
        <section class="da-card da-card-pad">
            <h2 class="da-subtitle">Cadastrar Marca</h2>

            <form asp-action="Create" method="post" class="da-stack">
                <div class="da-field">
                    <label class="da-label" for="Nome">Nome da Marca</label>
                    <input class="da-input" id="Nome" name="Nome" placeholder="Digite o nome da marca" required />
                </div>
                <button type="submit" class="da-btn da-btn-primary da-btn-wide">Inserir Marca</button>
            </form>
        </section>

        <section class="da-card da-card-pad">
            <h2 class="da-subtitle">Marcas Existentes</h2>

            @if (marcas.Any())
            {
                <div class="da-list-rows">
                    @foreach (var item in marcas)
                    {
                        <div class="da-list-row">
                            <span class="da-list-name">@item.Nome</span>
                            <a asp-action="Delete" asp-route-id="@item.Id" class="da-link-danger">Remover</a>
                        </div>
                    }
                </div>
            }
            else
            {
                <div class="da-empty">Nenhuma marca cadastrada.</div>
            }

            <div class="da-nav-row">
                <a asp-controller="Categoria" asp-action="Index" class="da-btn da-btn-outline">Ir para Categorias</a>
                <a asp-controller="Modelo" asp-action="Index" class="da-btn da-btn-outline">Ir para Modelos</a>
            </div>
        </section>
    </div>
</div>
